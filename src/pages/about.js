import { Component } from 'react';
import Home from './home'

export default class about extends Component {
  render() {
    // console.log(this.id);
    return (
      <div>
      </div>
    )
  }
}
